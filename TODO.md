# KFlow Code：做中学 TODO

> 目标不是尽快堆出一个“像 Codex 的壳”，而是亲手理解一个编程智能体为何能够可靠地观察、决策、行动和验证。任何抽象都必须由已经出现的真实问题换来。

## 使用方法

- 一次只推进一个编号任务；当前任务未验收，不开启下一项。
- 每个任务都必须完成：**问题 → 假设 → 最小实现 → 证据 → 笔记 → 复盘**。
- 不以“代码写完”为完成标准，只以可复现证据为准。
- AI 可以解释、审查和补充反例，但我必须先写下自己的判断。
- 每完成一项，在本文件勾选，并把结论写入 `docs/learning-log.md`。

## 第一性原理检查表（每一步必答）

1. 用户真正需要解决的问题是什么？
2. 若删掉框架、SDK 和术语，最小机制是什么？
3. 系统必须保持哪些不变量（安全、正确性、可终止性）？
4. 它会怎样失败？失败是否可观察、可恢复？
5. 什么证据能证明它工作，而不是“看起来能跑”？
6. 当前抽象解决了哪个已经发生的问题？若没有，先不抽象。

## 学习资产目录

- [x] 创建 `docs/vision.md`：项目目标、非目标、成功标准。
- [x] 创建 `docs/learning-log.md`：持续追加学习记录。
- [x] 创建 `docs/decisions/`：保存 `ADR-XXXX-*.md`。
- [x] 创建 `docs/experiments/`：记录真实仓库任务及工具轨迹。
- [x] 创建 `docs/reviews/`：每阶段一份 `P0-review.md` 等复盘。

## 学习展示基础设施

- [x] **LR.1 建立 LR Machine**：从真实项目文件生成进度、学习日志、KFC API 和 Git 状态页面。
- [x] **LR.2 建立 HTML 快照**：每项开发任务结束时生成自包含、不可变的学习证据。
- [x] **LR.3 建立安全边界**：服务仅监听本机、只读数据、拒绝路径穿越和浏览器写操作。
- [x] **LR.4 完成浏览器验收**：验证桌面端、390px 窄屏、控制台和安全路由。
- [x] **LR.5 归档核心代码**：在实时页面与离线快照中保存 `src/**/*.ts`、职责、行号和截断状态。

每条学习记录使用以下结构：

```md
## YYYY-MM-DD / 任务编号

- 第一性问题：
- 我的初始假设：
- 最小实验：
- 观察到的证据：
- 假设哪里错了：
- 得到的可复用原则：
- 尚未理解：
- 下一步：
```

## P0：工程骨架与边界

**核心问题：一个 CLI 程序最少需要哪些边界，才能被运行、测试和诊断？**

- [x] **P0.1 定义范围**：写 `docs/vision.md`，明确首版只做 CLI、单 Provider、单 Agent；不做 TUI、插件、MCP、多 Agent。
- [x] **P0.2 初始化工程**：创建 Node.js 22+、TypeScript、pnpm 项目；加入 Vitest、ESLint、Prettier。
- [x] **P0.3 建立最小入口**：实现 `kfc --help` 与 `kfc --version`，分离 CLI 参数解析和核心逻辑。
- [x] **P0.4 配置边界**：用 Zod 校验环境变量和单 Provider 配置；禁止输出密钥。
- [x] **P0.5 错误边界**：定义 `KfcError`，区分配置错误、Provider 错误和用户中断。
- [x] **P0.6 健康检查**：实现 `kfc doctor`，检查 Node 版本、配置文件和 API Key 是否存在。
- [x] **P0.7 写首个 ADR**：`ADR-0001-typescript-first.md`，记录选择 TypeScript 的收益、代价和重新评估条件。
- [x] **P0.8 阶段验收**：运行 `pnpm build && pnpm lint && pnpm test`；记录命令、输出和一次失败案例。
- [x] **P0.9 阶段复盘**：完成 `docs/reviews/P0-review.md`，确认每项验收都有证据。
- [x] **P0.10 DIY Quickstart**：实现 `kfc --quickstart` / `--qs`，交互写入自定义 OpenAI-compatible 非秘密配置并复用 Doctor。

**完成门槛：** 无 API Key 时错误清晰、无堆栈和密钥泄露；三个质量命令全部通过。

## P1：单轮模型调用

**核心问题：如何把不稳定的远程模型变成边界清晰、可取消、可观测的依赖？**

- [x] 定义内部 `ModelProvider`、请求、事件和错误类型。
- [x] 只实现一个 OpenAI-compatible Provider，不让 CLI 依赖供应商 SDK 类型。
- [x] 首个真实回归目标使用 `deepseek-v4-flash`；供应商差异留在 Provider 适配层，不进入 Core。
- [x] 实现流式输出、超时和 `AbortController` 中断。
- [x] 标准化认证、额度、限流、超时、上下文超限和服务端错误。
- [x] 用 Mock Provider 测试流式事件、中断与失败，不调用真实 API。
- [x] 增加 `openai-responses` 协议适配器，继续复用内部 `ModelProvider` 契约。
- [ ] 增加 `anthropic-messages` 协议适配器，验证跨协议边界。（已延期，不作为当前 P1 完成门槛。）
- [x] 通过最小 `kfc ask` 调用记录首 Token 延迟、总耗时和 Token 用量。
- [x] 完成真实 `kfc ask` 实验、ADR、学习日志和 P1 复盘。

## P2：最小 Agent Loop 与只读工具

**核心问题：Agent 的本质为何是可中断的受控状态机，而不是一段更长的 Prompt？**

- [x] 定义内部消息、Tool Call、Tool Result 和可选的显式步数预算；生产长任务模式不设置固定步数上限。
- [x] 实现 Tool Registry 与 Zod 参数校验。
- [x] 实现 `list_directory`、`read_file`、`grep`。
- [x] 注册 Codex、pi、Claude Code 的共同工具面：`find_files`、`apply_patch`、`write_file`、`shell`，并按 read/edit/execute 分层，修改和执行默认关闭。
- [x] 防止路径穿越，限制文件大小和结果数量。
- [x] 检测重复调用；工具失败应成为结构化结果，而非进程崩溃。
- [x] 接通 Chat Completions Tool Calling，并以显式 `kfc agent` 完成真实只读工作区验收。
- [x] 建立 TTY-only KFLOW 工作台：会话时间线、滚动、只读 Agent、状态面板、命令菜单、确认式上下文清除和安全终端恢复。
- [x] 增加模型思考与工具执行的实时 spinner 动效，并在完成、中断、错误和退出时清理计时器。
- [x] 接入 Agent Tool Result 事件，在 TUI 时间线显示安全的数量、退出码、耗时、错误和截断摘要。
- [x] 为助手 Markdown 增加轻量终端投影，并用低亮度分割线区分多轮问答。
- [x] 增加全宽数字雨与垂直居中的七行亮白 ASCII `KFLOW CODE` 启动动画，窄终端安全降级。
- [x] 集中管理 Agent 运行时设置、交互命令目录和工具展示标签，避免 CLI 与 TUI 重复维护。
- [x] 增加 `/themes` 主题菜单：实时切换、立即重绘，并持久化到用户配置。
- [x] 用脚本化 Mock Provider 确定性重放完整循环。
- [ ] 在陌生 TypeScript 仓库回答配置来源并给出文件与行号。
- [ ] 保存工具轨迹、ADR、学习日志和 P2 复盘。

## P3：写操作、Shell 与最小权限

**核心问题：模型提出动作、程序授权动作、工具执行动作，三者如何分权？**

- [x] 实现结构化 `apply_patch`，禁止静默整文件覆盖。
- [x] 实现受限 Shell：工作区 cwd、超时、输出上限和退出码。
- [x] 建立观察、修改、执行三档工具策略；修改和执行必须通过 `/tool` 显式开启。
- [x] 为已启用的修改与执行工具增加方向键逐次确认（`Yes` / `No` / `Tell me why?`）；拒绝和说明请求以结构化结果回灌模型。
- [x] 实现只读 `git_diff` 检查：文件级状态、增删摘要、未跟踪文件和会话开始前脏状态。
- [x] 增加失败后的恢复说明：报告已完成/失败工具、工作区 changed/unknown/unchanged 和安全检查路径；不自动回滚。
- [x] 完成一次“读取—修改—git diff—测试—解释”的真实临时 Git 工作区任务。
- [x] 记录被拒绝操作、失败恢复、ADR、实验和 P3 复盘。

## P4：事件驱动 Core 与会话持久化

**核心问题：哪些事实属于核心状态，哪些只是 UI 展示？**

- [ ] 用领域事件统一模型、工具、权限和完成状态。
- [ ] CLI 只消费事件，不反向控制 Core。
- [ ] 使用 JSONL 持久化会话，支持崩溃后读取与诊断。
- [ ] 测试事件顺序、追加写入和损坏记录处理。
- [ ] 完成 ADR、学习日志和 P4 复盘。

## P5：上下文与长会话

**核心问题：有限上下文中，什么信息值得保留，谁决定丢弃什么？**

- [ ] 区分系统规则、用户目标、代码证据、工具结果和历史摘要。
- [ ] 实现 Token 预算、输出截断、摘要和按需文件读取。
- [ ] 建立固定长任务，比较截断前后的正确性与成本。
- [ ] 记录错误遗忘、错误摘要和上下文污染案例。
- [ ] 完成 ADR、学习日志和 P5 复盘。

## P6：可验证完成

**核心问题：谁有资格宣布任务完成，模型的声明为何不能作为证据？**

- [ ] 将目标拆成可检查验收条件。
- [ ] 为每个条件绑定测试、Diff、静态检查或人工确认。
- [ ] 用退出码和结构化规则更新状态，模型不得自行标记通过。
- [ ] 输出 Completion Report：改动、证据、未验证项、风险和恢复方式。
- [ ] 测试“代码看似正确但测试失败”的拒绝完成路径。
- [ ] 完成 ADR、学习日志和 P6 复盘。

## P7–P8：扩展能力（锁定区）

> 只有 P1–P6 的单 Agent 闭环稳定后才解锁。扩展不能掩盖核心边界混乱。

- [ ] **P7**：按 Skills → Hooks → MCP 的顺序逐一加入，每次只验证一种扩展机制。
- [ ] **P8**：最后研究 Subagents、会话分支与 Git Worktree，并用相同验收集比较收益和成本。
- [ ] 为每个扩展写“为何现在需要它”和“删除它会失去什么”。

## 阶段完成定义

一个阶段只有同时满足以下条件才能勾选完成：

- [ ] 自动化构建、Lint 和测试通过。
- [ ] 至少一个真实仓库任务通过。
- [ ] 验收证据已保存，不依赖主观感觉。
- [ ] ADR 记录了关键选择及代价。
- [ ] 学习日志写明错误假设和新理解。
- [ ] 复盘列出技术债、下一步，以及明确不做的内容。
- [ ] README 和实际行为一致，且没有新增未记录的高风险权限。

## 当前唯一任务

**P3.1 TUI 权限与展示强化**：已完成 Edit/Execute 工具逐次确认、结构化拒绝回灌、Markdown 投影和多轮问答分隔；下一步是读取—修改—测试—解释真实闭环、`git diff` 检查与失败恢复说明。
